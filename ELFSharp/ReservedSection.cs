using System.IO;

namespace ELFSharp
{
   /* public class ReservedSection : Section
    {
        internal ReservedSection(SectionHeader header, SpecialSectionType type) : base(header)
        {
            Type = type;
        }

        public SpecialSectionType Type { get; private set; }
    }*/
}